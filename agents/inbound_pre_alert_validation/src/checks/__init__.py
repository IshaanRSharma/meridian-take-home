"""The business rules, one module per Check card.

Both derive no capabilities, so both stay in workflow code and a failing eval
case fails for a logic reason rather than because a network was slow.
"""
