{
  "spec_checksum": "927c19a144c7db3d96aeb5b58b8cdbdd053c760b5aecba725d02d26ed8e51461",
  "$comment": [
    "Every decision the spec did not determine, with a prediction attached. When a",
    "case fails the first question is which assumption predicted it, rather than",
    "what this code does.",
    "",
    "prompted_by null is the signal: an assumption with no anchor in the spec is",
    "the most likely gap on the board. Seven of these are null. The two that are",
    "gaps in what the board can PRODUCE are listed first, because every case will",
    "fail on them; the rest are thresholds and defaults the sweep can tune."
  ],
  "assumptions": [
    {
      "id": "asn_column_has_no_source",
      "decision": "did not emit `invoices_mismatched_asn`",
      "prompted_by": null,
      "because": "no entity carries an ASN, no check reads one, and no field on the board holds one. A number here would be invented rather than inferred",
      "falsified_if": "every case fails on this column. The likely source is the per-invoice .xlsx in the mailbox, which is a question for the process owner"
    },
    {
      "id": "match_condition_orthographic",
      "decision": "matched the subject phrase case-insensitively, collapsing separators and accepting 'Pre Alerts Documents' and 'Pre-Alert Documentation' alongside the quoted form",
      "prompted_by": "primitives.prealert_received.match_condition",
      "because": "the spec quotes a phrase, and prose on a card describes how to recognise something rather than specifying a byte comparison. Read literally the quoted phrase matches zero of the fourteen real messages",
      "falsified_if": "a real pre-alert is skipped, or a message that is not one is processed"
    },
    {
      "id": "correlation_key_read_from_the_body",
      "decision": "took the container number from the message body under a `Container No` label, falling back to a bare ISO container pattern",
      "prompted_by": "primitives.prealert_received.correlation_key",
      "because": "the spec names commercial_invoice.container_no, which is true of the finished data and unavailable at trigger time \u2014 correlating is what decides which workflow an attachment belongs to, and extraction has not run yet. The body carries it in 13 of 14 messages; the subject in 1 and a filename in 5. Three shipments arrive as two emails split by document type, one carrying the certificates and the other the invoice, and the certificate-only email names the container nowhere but the body \u2014 so any other reading orphans those certificates and reports them missing while they sit in the inbox",
      "falsified_if": "two shipments merge into one workflow, one splits across two, or a message whose body names no container is silently dropped rather than reported"
    },
    {
      "id": "air_waybill_shipments_are_not_correlated",
      "decision": "a message naming no container is left ungrouped rather than keyed on its air waybill",
      "prompted_by": "primitives.prealert_received.correlation_key",
      "because": "one historical shipment is an air waybill and the board's correlation key is a container number. Nothing says how an air shipment correlates",
      "falsified_if": "an air-freight pre-alert has to be processed. The one such row in the eval set is absent from the mailbox, so nothing exercises this"
    },
    {
      "id": "batch_matching_is_exact",
      "decision": "compared batch numbers exactly, with no trimming and no case folding",
      "prompted_by": "primitives.coas_valid.criteria[0].op",
      "because": "each_has_matching is a typed operator with defined semantics, and every context block on this board is empty \u2014 no assertion loosens it. An earlier build folded case and trimmed here and cited primitives.coas_valid.context.inherited[0] as approval; that path does not exist in this spec and never has at this checksum. The looser rule was also unfalsifiable, because it passes everything the exact one passes, so no eval case could report that it had been applied",
      "falsified_if": "a shipment reports missing_coa for a batch whose certificate is present but spelled differently \u2014 the bundle shows the pair, and normalising is then a repair somebody approved. On the three fixture cases exact and normalised agree exactly, so nothing currently exercises the difference"
    },
    {
      "id": "instances_deduplicated_on_scalar_content",
      "decision": "collapsed repeated readings of one document on its scalar fields, ignoring list fields",
      "prompted_by": null,
      "because": "a shipment arrives as several emails and a sender resends a whole set rather than a diff, so the same certificate arrives twice under different filenames and only its content identifies it \u2014 nothing on the board declares an identity for an entity. Counting per arrival would multiply every count by how many times the process woke up, which is wrong in a way that looks plausible because the numbers stay consistent with each other. Lists are excluded because a partial reading differs from a whole one on exactly those, which would make every fragment its own document",
      "falsified_if": "two genuinely distinct documents agreeing on every scalar field collapse into one, or a count comes back a small integer multiple of the expected one"
    },
    {
      "id": "unmatched_batches_report_missing_coa",
      "decision": "every unmatched batch reports `missing_coa`; `mismatched_coa` is never reached",
      "prompted_by": "primitives.coas_valid.outcomes",
      "because": "three outcomes are declared and one each_has_matching criterion distinguishes two results. Nothing in the criteria separates 'a batch has no COA' from 'a COA exists but disagrees', and inventing a similarity threshold would be inventing a rule. Both outcomes route to report_coa_discrepancy on this board, so the label is not observable",
      "falsified_if": "an eval column distinguishes the two, or the two outcomes are ever wired to different actions"
    },
    {
      "id": "repeat_waits_for_new_documents",
      "decision": "a repeat edge is followed only when more has arrived since the last pass",
      "prompted_by": "edges.e7,e8.relation",
      "because": "the cycle exists because corrected paperwork arrives days later. Re-running a check immediately against inputs that have not changed reaches the same conclusion forever, and nothing on the board bounds the loop",
      "falsified_if": "a shipment stops before a check that should have re-run"
    },
    {
      "id": "max_passes",
      "decision": "12",
      "prompted_by": null,
      "because": "a safety net, not a business rule. The loop already terminates when nothing new arrives; this stops a mis-drawn cycle becoming a workflow Temporal retries forever with no traceback",
      "falsified_if": "a legitimate shipment needs more than twelve steps"
    },
    {
      "id": "document_boundaries_are_page_headers",
      "decision": "a page whose header carries a recognition phrase opens a document; pages after it belong to it until the next header",
      "prompted_by": "entities.*.identified_by",
      "because": "every identified_by on this board is page-level \u2014 'the page header reads ...'. One attachment is a 37-page bundle of an invoice and its certificates and another is a single certificate; the same rule handles both without either declaring which it is",
      "falsified_if": "a multi-page invoice splits into several, or a bundle collapses into one"
    },
    {
      "id": "text_layer_before_vision",
      "decision": "read the PDF text layer, and send to a vision model only when no page yields 40 characters",
      "prompted_by": null,
      "because": "the corpus is mixed and most of it is native PDF. Sending everything to vision is slower and less accurate; refusing the scanned bundle loses a whole shipment",
      "falsified_if": "a document with a junk text layer is read as text and extracts nothing"
    },
    {
      "id": "confidence_floor",
      "decision": "0.6, the scaffold default, left unchanged",
      "prompted_by": null,
      "because": "nothing in the spec sets one. A certificate of compliance reads almost exactly like a certificate of analysis, and extracting against the wrong schema is worse than declining",
      "falsified_if": "a real certificate is declined, or a compliance certificate is counted as an analysis one"
    },
    {
      "id": "recipients_resolved_outside_the_workflow",
      "decision": "roles are resolved to addresses before the workflow starts and passed in as input",
      "prompted_by": "primitives.report_coa_discrepancy.recipients",
      "because": "an identity in a checksummed spec would make a personnel change force a new version. Resolving at the edge keeps the spec clean and the resolution replayable",
      "falsified_if": "a role has no binding and the failure surfaces as a missing recipient rather than a named binding error"
    },
    {
      "id": "idempotency_from_the_cards_own_words",
      "decision": "built the key from container, the failing values and the outcome, as the card's idempotency_key names them",
      "prompted_by": "primitives.report_coa_discrepancy.idempotency_key",
      "because": "the card says 'container_no + batch_no + discrepancy_kind' in prose. Hashing exactly those makes one more batch arriving change the key, so a new discrepancy goes out and a repeat of the same one does not",
      "falsified_if": "a supervisor is told the same thing twice, or a genuinely new discrepancy is suppressed"
    },
    {
      "id": "nothing_is_sent",
      "decision": "every capability resolves to a recorder; no email leaves and no system is written to",
      "prompted_by": null,
      "because": "the graded artefact is the row the process produces. A recorded call is assertable where a delivered message is a screenshot, and shadow mode removes the only irreversible operation in the system",
      "falsified_if": "a demo needs a real send, which is a mode flag rather than a change"
    },
    {
      "id": "every_check_reports_regardless_of_routing",
      "decision": "ran every Check and wrote its counts into the row, even when the graph terminated at an exception exit before reaching it",
      "prompted_by": "edges: invoice_complete -> report_invoice_discrepancy (is_terminal)",
      "because": "the board draws a failing invoice as ending the shipment, so coas_valid never ran and three columns came back null. Every historical row carries BOTH invoice counts and COA counts, including rows with invoice failures \u2014 so a discrepancy is one finding among several, not a terminal state. Decided against the corpus rather than the drawing.",
      "falsified_if": "a shipment that genuinely should stop at the first discrepancy reports COA counts it never should have computed. The board disagrees with this code until it is re-frozen; raised as a spec gap."
    },
    {
      "id": "status_derived_from_check_failures",
      "decision": "set status to ACTIVE when any check reported a failure and RESOLVED otherwise",
      "prompted_by": null,
      "because": "no card fills status. The rule fits all nine historical rows, and MNBU3974949 rules out ASN being part of it: 14 mismatched ASNs and RESOLVED.",
      "falsified_if": "a shipment with no check failure is ACTIVE, or one with a failure is RESOLVED. Nobody on the board authorised this column; raised as a spec gap."
    },
    {
      "id": "batches_come_from_the_declared_block",
      "decision": "read each invoice's batch numbers from its labelled `BATCH NOS:` block rather than from whatever the extractor put in line_items[].batch_no",
      "prompted_by": "primitives.coas_valid.criteria[0].left",
      "because": "the invoice does not carry a batch number per line item. It carries one block listing every lot, and the extraction schema asks for line_items[].batch_no \u2014 a shape the document does not have. Asked for something absent the model fills it anyway: right on the page holding the block, null on continuation pages, and on a totals page the nearest number, which is how 3291840 became a batch nobody had a certificate for. The block is labelled and unambiguous, so reading it is taking a field off a page rather than deciding what a batch is",
      "falsified_if": "an invoice states a BATCH NOS block that disagrees with its line items, or a supplier writes the list under a different label \u2014 the block is then not authoritative and the count follows the wrong source silently, because a wrong batch list still produces a self-consistent row"
    }
  ]
}
