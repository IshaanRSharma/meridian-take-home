"""The Checks this board declares, one module each.

One file per primitive so a repair touches one file — a rule about blast radius,
not about taste: two checks in one module means a fix to one can break the other
and the gate has no way to tell them apart.
"""
